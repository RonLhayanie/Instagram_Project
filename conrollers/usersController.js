//create router
const express = require('express')
const router  = express.Router() 

// add all needed http methods to the router
// such as router.get('/create')
// note: the final call will be /users/create so it doesnt need to be specific

module.exports = router